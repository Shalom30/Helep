apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      containers:
        - name: analytics-service
          image: {{ .Values.services.analytics.image }}
          ports:
            - containerPort: {{ .Values.services.analytics.port }}
          env:
            - name: KAFKA_BOOTSTRAP
              value: {{ .Values.kafkaBootstrap }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: helep-secret
                  key: JWT_SECRET
            - name: DB_PATH
              value: /data/analytics.db
          livenessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.services.analytics.port }}
          readinessProbe:
            httpGet:
              path: /readyz
              port: {{ .Values.services.analytics.port }}
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
spec:
  selector:
    app: analytics-service
  ports:
    - port: {{ .Values.services.analytics.port }}
      targetPort: {{ .Values.services.analytics.port }}